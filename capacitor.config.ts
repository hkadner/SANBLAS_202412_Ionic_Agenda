import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lafabricadeandroides.agendasanblas',
  appName: 'Agenda SanBlas',
  webDir: 'www'
};

export default config;
